{
  "name": "api-ingestor",
  "version": "1.0.0",
  "description": "Simple HTTP API that accepts incoming alerts on `POST /alerts` and forwards them to the Alert Processor service.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.13.2",
    "express": "^5.1.0"
  }
}
